Web Client Server
===============================

# Installation

cd /authorization-server
npm install
node app.js
```

Then install and run the resource server
```
cd /resource-server
npm install
node app.js
```

Finally, run this server
```
cd /web-client
npm install
node app.js
```
